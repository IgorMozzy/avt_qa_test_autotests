# Тест-кейсы

## Создание объявления - POST /api/1/item


| ID | Заголовок | Предусловия | Шаги | Ожидаемый результат  |
| ------------- | ----------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------  |
| **CREATE-1** | Создание объявления с валидными данными | - | 1. Отправить POST-запрос на `/api/1/item` с телом: `{"sellerID": 111111, "name": "Телефон", "price": 50000, "statistics": {"likes": 10, "viewCount": 100, "contacts": 5}}`.<br>2. Проверить статус-код ответа.<br>3. Проверить, что в ответе присутствуют поля: id, sellerId, name, price, statistics, createdAt.<br>4. Сверить значения полей sellerId, name, price, statistics с отправленными. | Статус-код 200. Тело ответа содержит все поля. Значения sellerId, name, price, statistics совпадают с отправленными. Поле id содержит уникальный идентификатор. Поле createdAt содержит дату создания.  |
| **CREATE-4** | Создание объявления с нулевой статистикой | - | 1. Отправить POST-запрос на `/api/1/item` с `"statistics": {"likes": 0, "viewCount": 0, "contacts": 0}` и валидными sellerID, name, price.<br>2. Проверить статус-код ответа.<br>3. Проверить значения полей статистики в ответе. | Статус-код 200. Все поля статистики равны 0.  |
| **CREATE-5** | Неидемпотентность POST: два одинаковых запроса создают разные объявления | - | 1. Взять одно и то же полное валидное тело (поле `statistics` обязательно для успешного создания, см. CREATE-55), например: `{"sellerID": 111111, "name": "Тест", "price": 1000, "statistics": {"likes": 1, "viewCount": 1, "contacts": 1}}`.<br>2. Отправить POST-запрос на `/api/1/item` с этим телом.<br>3. Отправить точно такой же POST-запрос повторно с тем же JSON.<br>4. Сравнить поле id в обоих ответах. | Статус-код 200 для обоих запросов. Поля id различаются - каждый запрос создает новое объявление (POST не идемпотентен).  |
| **CREATE-5a** | Ответ на создание объявления содержит полный объект | - | 1. Отправить POST-запрос на `/api/1/item` с валидным телом.<br>2. Проверить, что ответ содержит полный объект `{id, sellerId, name, price, statistics, createdAt}`, а не строку вида `{"status": "Сохранили объявление - <uuid>"}`. | Статус-код 200. Тело ответа - полный объект объявления с полями id, sellerId, name, price, statistics, createdAt.  |
| **CREATE-6** | Создание объявления без поля name | - | 1. Отправить POST-запрос на `/api/1/item` с телом: `{"sellerID": 111111, "price": 1000}` (поле name отсутствует). | Статус-код 400. Сервер отклоняет запрос.  |
| **CREATE-7** | Создание объявления без поля sellerID | - | 1. Отправить POST-запрос на `/api/1/item` с телом: `{"name": "Тест", "price": 1000}` (поле sellerID отсутствует). | Статус-код 400. Сервер отклоняет запрос.  |
| **CREATE-8** | Создание объявления без поля price | - | 1. Отправить POST-запрос на `/api/1/item` с телом: `{"sellerID": 111111, "name": "Тест"}` (поле price отсутствует). | Статус-код 400. Сервер отклоняет запрос.  |
| **CREATE-9** | Создание объявления с пустым телом | - | 1. Отправить POST-запрос на `/api/1/item` с пустым телом `{}`. | Статус-код 400. Сервер отклоняет запрос.  |
| **CREATE-10** | Создание объявления с пустым объектом statistics | - | 1. Отправить POST-запрос на `/api/1/item` с телом: `{"sellerID": 111111, "name": "Тест", "price": 1000, "statistics": {}}`. | Статус-код 400. Объект statistics не содержит обязательных полей.  |
| **CREATE-11** | Создание объявления без поля likes в statistics | - | 1. Отправить POST-запрос на `/api/1/item` с телом: `{"sellerID": 111111, "name": "Тест", "price": 1000, "statistics": {"viewCount": 0, "contacts": 0}}`. | Статус-код 400. Поле likes обязательно внутри statistics.  |
| **CREATE-12** | Создание объявления без поля viewCount в statistics | - | 1. Отправить POST-запрос на `/api/1/item` с телом: `{"sellerID": 111111, "name": "Тест", "price": 1000, "statistics": {"likes": 0, "contacts": 0}}`. | Статус-код 400. Поле viewCount обязательно внутри statistics.  |
| **CREATE-13** | Создание объявления без поля contacts в statistics | - | 1. Отправить POST-запрос на `/api/1/item` с телом: `{"sellerID": 111111, "name": "Тест", "price": 1000, "statistics": {"likes": 0, "viewCount": 0}}`. | Статус-код 400. Поле contacts обязательно внутри statistics.  |
| **CREATE-14** | Создание объявления с отрицательной ценой | - | 1. Отправить POST-запрос на `/api/1/item` с телом: `{"sellerID": 111111, "name": "Тест", "price": -100}`. | Статус-код 400. Цена не может быть отрицательной.  |
| **CREATE-15** | Создание объявления с ценой = 0 | - | 1. Отправить POST-запрос на `/api/1/item` с телом: `{"sellerID": 111111, "name": "Тест", "price": 0}`. | Статус-код 400. Цена должна быть положительным числом.  |
| **CREATE-16** | Создание объявления с sellerID = 0 | - | 1. Отправить POST-запрос на `/api/1/item` с телом: `{"sellerID": 0, "name": "Тест", "price": 1000, "statistics": {"likes": 1, "viewCount": 1, "contacts": 1}}`. | Статус-код 400. Ноль не является допустимым идентификатором продавца.  |
| **CREATE-17** | Создание объявления с минимальной ценой (price = 1) | - | 1. Отправить POST-запрос на `/api/1/item` с `"price": 1` и валидными sellerID, name.<br>2. Проверить статус-код ответа. | Статус-код 200. Объявление создано с price = 1.  |
| **CREATE-18** | Создание объявления с отрицательным sellerID | - | 1. Отправить POST-запрос на `/api/1/item` с телом: `{"sellerID": -111111, "name": "Тест", "price": 1000}`. | Статус-код 400. sellerID не может быть отрицательным.  |
| **CREATE-23** | Создание объявления с sellerID = 2^53+1 (переполнение) | - | 1. Отправить POST-запрос на `/api/1/item` с `"sellerID": 9007199254740993`. | Статус-код 400. Значение sellerID выходит за допустимый диапазон.  |
| **CREATE-19** | Создание объявления с отрицательным количеством лайков | - | 1. Отправить POST-запрос на `/api/1/item` с телом: `{"sellerID": 111111, "name": "Тест", "price": 1000, "statistics": {"likes": -1, "viewCount": 0, "contacts": 0}}`. | Статус-код 400. Количество лайков не может быть отрицательным.  |
| **CREATE-20** | Создание объявления с отрицательным количеством просмотров | - | 1. Отправить POST-запрос на `/api/1/item` с телом: `{"sellerID": 111111, "name": "Тест", "price": 1000, "statistics": {"likes": 0, "viewCount": -1, "contacts": 0}}`. | Статус-код 400. Количество просмотров не может быть отрицательным.  |
| **CREATE-21** | Создание объявления с отрицательным количеством контактов | - | 1. Отправить POST-запрос на `/api/1/item` с телом: `{"sellerID": 111111, "name": "Тест", "price": 1000, "statistics": {"likes": 0, "viewCount": 0, "contacts": -1}}`. | Статус-код 400. Количество контактов не может быть отрицательным.  |
| **CREATE-22** | Создание объявления с ценой 2^53+1 (переполнение) | - | 1. Отправить POST-запрос на `/api/1/item` с `"price": 9007199254740993`. | Статус-код 400. Значение price выходит за допустимый диапазон.  |
| **CREATE-24** | Создание объявления с пустым именем | - | 1. Отправить POST-запрос на `/api/1/item` с `"name": ""`. | Статус-код 400. Пустая строка не является допустимым именем.  |
| **CREATE-25** | Создание объявления с именем из пробелов | - | 1. Отправить POST-запрос на `/api/1/item` с `"name": " "`. | Статус-код 400. Строка из пробелов не является допустимым именем.  |
| **CREATE-26** | Создание объявления с sellerID строкой | - | 1. Отправить POST-запрос на `/api/1/item` с телом: `{"sellerID": "not_a_number", "name": "Тест", "price": 1000}`. | Статус-код 400. sellerID должен быть числом.  |
| **CREATE-27** | Создание объявления с name числом | - | 1. Отправить POST-запрос на `/api/1/item` с телом: `{"sellerID": 111111, "name": 12345, "price": 1000}`. | Статус-код 400. name должен быть строкой.  |
| **CREATE-28** | Создание объявления с price строкой | - | 1. Отправить POST-запрос на `/api/1/item` с телом: `{"sellerID": 111111, "name": "Тест", "price": "not_a_number"}`. | Статус-код 400. price должен быть числом.  |
| **CREATE-29** | Создание объявления с statistics строкой | - | 1. Отправить POST-запрос на `/api/1/item` с телом: `{"sellerID": 111111, "name": "Тест", "price": 1000, "statistics": "not_an_object"}`. | Статус-код 400. statistics должен быть объектом.  |
| **CREATE-30** | Создание объявления с sellerID дробным числом | - | 1. Отправить POST-запрос на `/api/1/item` с телом: `{"sellerID": 111111.5, "name": "Тест", "price": 1000, "statistics": {...}}`. | Статус-код 400. sellerID должен быть целым числом.  |
| **CREATE-31** | Создание объявления с price дробным числом | - | 1. Отправить POST-запрос на `/api/1/item` с телом: `{"sellerID": 111111, "name": "Тест", "price": 99.99, "statistics": {...}}`. | Статус-код 400. price должен быть целым числом.  |
| **CREATE-32** | Создание объявления с likes строкой в statistics | - | 1. Отправить POST-запрос на `/api/1/item` с телом: `{"sellerID": 111111, "name": "Тест", "price": 1000, "statistics": {"likes": "abc", "viewCount": 0, "contacts": 0}}`. | Статус-код 400. likes должен быть числом.  |
| **CREATE-33** | Создание объявления с viewCount строкой в statistics | - | 1. Отправить POST-запрос на `/api/1/item` с телом: `{"sellerID": 111111, "name": "Тест", "price": 1000, "statistics": {"likes": 0, "viewCount": "abc", "contacts": 0}}`. | Статус-код 400. viewCount должен быть числом.  |
| **CREATE-34** | Создание объявления с contacts строкой в statistics | - | 1. Отправить POST-запрос на `/api/1/item` с телом: `{"sellerID": 111111, "name": "Тест", "price": 1000, "statistics": {"likes": 0, "viewCount": 0, "contacts": "abc"}}`. | Статус-код 400. contacts должен быть числом.  |
| **CREATE-35** | Создание объявления с likes дробным числом в statistics | - | 1. Отправить POST-запрос на `/api/1/item` с `"statistics": {"likes": 1.5, "viewCount": 0, "contacts": 0}`. | Статус-код 400. likes должен быть целым числом.  |
| **CREATE-36** | Создание объявления с viewCount дробным числом в statistics | - | 1. Отправить POST-запрос на `/api/1/item` с `"statistics": {"likes": 0, "viewCount": 1.5, "contacts": 0}`. | Статус-код 400. viewCount должен быть целым числом.  |
| **CREATE-37** | Создание объявления с contacts дробным числом в statistics | - | 1. Отправить POST-запрос на `/api/1/item` с `"statistics": {"likes": 0, "viewCount": 0, "contacts": 1.5}`. | Статус-код 400. contacts должен быть целым числом.  |
| **CREATE-38** | Создание объявления с sellerID как null | - | 1. Отправить POST-запрос на `/api/1/item` с телом: `{"sellerID": null, "name": "Тест", "price": 1000, "statistics": {...}}`. | Статус-код 400. null не является допустимым значением для числового поля.  |
| **CREATE-39** | Создание объявления с name = null | - | 1. Отправить POST-запрос на `/api/1/item` с телом: `{"sellerID": 111111, "name": null, "price": 1000, "statistics": {...}}`. | Статус-код 400. null не является допустимым значением для строкового поля.  |
| **CREATE-40** | Создание объявления с price = null | - | 1. Отправить POST-запрос на `/api/1/item` с телом: `{"sellerID": 111111, "name": "Тест", "price": null, "statistics": {...}}`. | Статус-код 400. null не является допустимым значением для числового поля.  |
| **CREATE-41** | Создание объявления с statistics = null | - | 1. Отправить POST-запрос на `/api/1/item` с телом: `{"sellerID": 111111, "name": "Тест", "price": 1000, "statistics": null}`. | Статус-код 400. null не является допустимым значением для объектного поля.  |
| **CREATE-42** | Создание объявления с statistics в виде массива | - | 1. Отправить POST-запрос на `/api/1/item` с телом: `{"sellerID": 111111, "name": "Тест", "price": 1000, "statistics": []}`. | Статус-код 400. statistics должен быть объектом, а не массивом.  |
| **CREATE-43** | Создание объявления с телом запроса в виде массива | - | 1. Отправить POST-запрос на `/api/1/item` с телом `[]`. | Статус-код 400. Тело запроса должно быть объектом, а не массивом.  |
| **CREATE-44** | Создание объявления с likes = null в statistics | - | 1. Отправить POST-запрос на `/api/1/item` с телом, где в `statistics` поле `likes` равно `null`, остальные поля статистики числа (например `viewCount: 0`, `contacts: 0`), остальное тело валидно. | Статус-код 400. null не является допустимым значением для числового поля.  |
| **CREATE-45** | Создание объявления с viewCount = null в statistics | - | 1. Отправить POST-запрос на `/api/1/item` с телом, где в `statistics` поле `viewCount` равно `null`, остальные поля статистики числа. | Статус-код 400. null не является допустимым значением для числового поля.  |
| **CREATE-46** | Создание объявления с contacts = null в statistics | - | 1. Отправить POST-запрос на `/api/1/item` с телом, где в `statistics` поле `contacts` равно `null`, остальные поля статистики числа. | Статус-код 400. null не является допустимым значением для числового поля.  |
| **CREATE-47** | Создание объявления с минимальной ненулевой статистикой | - | 1. Отправить POST-запрос на `/api/1/item` с `"statistics": {"likes": 1, "viewCount": 1, "contacts": 1}` и валидными sellerID, name, price.<br>2. Проверить статус-код ответа. | Статус-код 200. Объявление создано со статистикой = 1 для каждого поля.  |
| **CREATE-48** | Создание объявления со статистикой 2^53+1 (переполнение) | - | 1. Отправить POST-запрос на `/api/1/item` с `"statistics": {"likes": 9007199254740993, "viewCount": 9007199254740993, "contacts": 9007199254740993}`. | Статус-код 400. Значения статистики выходят за допустимый диапазон.  |
| **CREATE-49** | Создание объявления с sellerID = INT_MAX (2^31-1) | - | 1. Отправить POST-запрос на `/api/1/item` с `"sellerID": 2147483647` и валидными name, price. | Статус-код 200. Объявление создано (верхней границы для sellerID нет).  |
| **CREATE-50** | Security: Создание объявления с SQL-инъекцией в name | - | 1. Отправить POST-запрос на `/api/1/item` с `"name": "'; DROP TABLE items; --"` и валидными sellerID, price.<br>2. Получить созданное объявление по ID.<br>3. Сверить значение поля name. | Статус-код 200. Объявление создано; GET возвращает то же значение name, что передали в POST.  |
| **CREATE-51** | Security: Создание объявления с XSS в name | - | 1. Отправить POST-запрос на `/api/1/item` с `"name": "<script>alert(1)</script>"` и валидными sellerID, price.<br>2. Получить созданное объявление по ID.<br>3. Сверить значение поля name. | Статус-код 200. Объявление создано. GET возвращает name без изменений - строка хранится как есть.  |
| **CREATE-52** | Security: Создание объявления с null-байтом в name | - | 1. Отправить POST-запрос на `/api/1/item` с `"name": "test\x00test"` и валидными sellerID, price. | Статус-код 400. Null-байт не является допустимым символом в имени.  |
| **CREATE-53** | Создание объявления с лишним полем в корне тела | - | 1. Отправить POST-запрос на `/api/1/item` с валидным телом плюс `"unknownField": "value"` в корне. | Статус-код 200. Неизвестные поля в корне игнорируются.  |
| **CREATE-54** | Создание объявления с лишним полем в statistics | - | 1. Отправить POST-запрос на `/api/1/item` с валидным телом, в statistics добавить `"unknownField": "value"`. | Статус-код 200. Неизвестные поля в statistics игнорируются.  |
| **CREATE-55** | Создание объявления без поля statistics (объект целиком) | - | 1. Отправить POST-запрос на `/api/1/item` с телом: `{"sellerID": 111111, "name": "Тест", "price": 1000}` (поле statistics отсутствует). | Статус-код 400. Поле statistics обязательно.  |
| **CREATE-56** | Создание объявления с именем из 10000 символов | - | 1. Отправить POST-запрос на `/api/1/item` с `"name"`, содержащим 10000 символов. | Статус-код 400. Длина поля name должна быть ограничена.  |
| **CREATE-57** | Security: Создание объявления с составным эмодзи в name | - | 1. Отправить POST-запрос на `/api/1/item` с `name`, содержащим составной эмодзи (например флаг как два региональных индикатора в одной строке), и валидными sellerID, price. | Статус-код 200. Сложные символы Unicode принимаются без ошибки.  |


---

## Получение объявления - GET /api/1/item/:id


| ID | Заголовок | Предусловия | Шаги | Ожидаемый результат  |
| --------- | ------------------------------------------------------------------- | ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------  |
| **GET-1** | Получение существующего объявления по ID | Создано объявление, известен его ID. | 1. Отправить GET-запрос на `/api/1/item/{id}`, подставив ID созданного объявления.<br>2. Проверить статус-код ответа.<br>3. Проверить, что поля id, sellerId, name, price, statistics, createdAt присутствуют в ответе.<br>4. Сверить значения с данными, указанными при создании. | Статус-код 200. Все поля присутствуют и совпадают с данными созданного объявления.  |
| **GET-2** | Идемпотентность GET: повторный запрос возвращает тот же результат | Создано объявление, известен его ID. | 1. Отправить GET-запрос на `/api/1/item/{id}`.<br>2. Отправить тот же GET-запрос повторно.<br>3. Сравнить тела обоих ответов. | Статус-код 200 для обоих запросов. Тела ответов полностью идентичны.  |
| **GET-3** | Получение объявления с несуществующим ID | Создано объявление, известен его ID. | 1. Удалить объявление: DELETE `/api/2/item/{id}`.<br>2. Отправить GET-запрос на `/api/1/item/{id}`. | Статус-код 404.  |
| **GET-4** | Получение объявления с невалидным форматом ID | - | 1. Отправить GET-запрос на `/api/1/item/invalid-id-format`. | Статус-код 400.  |
| **GET-5** | GET /item/:id возвращает объект, а не массив | Создано объявление, известен его ID. | 1. Отправить GET-запрос на `/api/1/item/{id}`.<br>2. Проверить тип корневого элемента ответа. | Статус-код 200. Тело ответа является JSON-объектом `{...}`, а не массивом `[{...}]`.  |
| **GET-6** | Поле createdAt содержит валидную дату в формате ISO 8601 | Создано объявление, известен его ID. | 1. Отправить GET-запрос на `/api/1/item/{id}`.<br>2. Извлечь значение поля `createdAt`.<br>3. Проверить, что значение парсится стандартным ISO 8601 парсером. | Статус-код 200. Значение `createdAt` разбирается как дата в формате ISO 8601.  |
| **GET-7** | Security: GET /item/:id с SQL-инъекцией в ID возвращает 400, не 500 | - | 1. Отправить GET-запрос на `/api/1/item/%27%3B%20DROP%20TABLE%20items%3B%20--` (SQL-инъекция в path). | Статус-код 400, не 500.  |


---

## Получение объявлений продавца - GET /api/1/:sellerID/item


| ID | Заголовок | Предусловия | Шаги | Ожидаемый результат  |
| ------------ | ----------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------  |
| **SELLER-1** | Получение всех объявлений продавца | - | 1. Выбрать sellerID (например, 222222).<br>2. Создать два объявления с этим sellerID через POST `/api/1/item`.<br>3. Отправить GET-запрос на `/api/1/222222/item`.<br>4. Проверить, что оба созданных объявления присутствуют в ответе (сверить по ID). | Статус-код 200. Ответ содержит массив, включающий оба созданных объявления.  |
| **SELLER-2** | Получение объявлений у продавца без объявлений | - | 1. Выбрать sellerID, для которого не создавалось объявлений (например, 888888).<br>2. Отправить GET-запрос на `/api/1/888888/item`. | Статус-код 200. Ответ содержит пустой массив `[]`.  |
| **SELLER-3** | Получение объявлений с невалидным sellerID (строка) | - | 1. Отправить GET-запрос на `/api/1/not_a_number/item`. | Статус-код 400.  |
| **SELLER-4** | Получение объявлений с отрицательным sellerID | - | 1. Отправить GET-запрос на `/api/1/-111111/item`. | Статус-код 400.  |
| **SELLER-5** | Получение объявлений с дробным sellerID | - | 1. Отправить GET-запрос на `/api/1/111111.5/item`. | Статус-код 400. Дробное число не является допустимым идентификатором продавца.  |
| **SELLER-6** | Получение объявлений с sellerID = 2^53+1 | - | 1. Отправить GET-запрос на `/api/1/9007199254740993/item`. | Статус-код 400. Значение выходит за допустимый диапазон.  |
| **SELLER-7** | Список продавца не обрезается пагинацией при 50 объявлениях | - | 1. Создать 50 объявлений с одним sellerID.<br>2. Отправить GET-запрос на `/api/1/{sellerID}/item`.<br>3. Проверить, что ответ содержит ровно 50 элементов. | Статус-код 200. Ответ содержит все 50 объявлений без сокращения.  |


---

## Получение статистики - GET /api/1/statistic/:id, GET /api/2/statistic/:id


| ID | Заголовок | Предусловия | Шаги | Ожидаемый результат  |
| ---------- | --------------------------------------------------------------- | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------  |
| **STAT-1** | Получение статистики v1 | Создано объявление с известной статистикой (likes, viewCount, contacts). | 1. Отправить GET-запрос на `/api/1/statistic/{id}`.<br>2. Проверить статус-код ответа.<br>3. Сверить значения likes, viewCount, contacts с указанными при создании. | Статус-код 200. Значения статистики совпадают с отправленными при создании объявления.  |
| **STAT-2** | Получение статистики v2 | Создано объявление с известной статистикой. | 1. Отправить GET-запрос на `/api/2/statistic/{id}`.<br>2. Проверить статус-код ответа.<br>3. Сверить значения статистики. | Статус-код 200. Значения статистики совпадают с отправленными при создании.  |
| **STAT-3** | Статистика v1 и v2 возвращают одинаковые данные | Создано объявление. | 1. Отправить GET-запрос на `/api/1/statistic/{id}`.<br>2. Отправить GET-запрос на `/api/2/statistic/{id}`.<br>3. Сравнить тела обоих ответов. | Статус-код 200 для обоих запросов; тела ответов совпадают.  |
| **STAT-4** | GET статистики не изменяет счетчик просмотров | Создано объявление с известной статистикой (например, viewCount = 10). | 1. Отправить GET-запрос на `/api/1/statistic/{id}`. Зафиксировать значение viewCount.<br>2. Отправить тот же GET-запрос повторно.<br>3. Сравнить viewCount в обоих ответах. | Статус-код 200 для обоих запросов. Значение viewCount не изменилось; повторный GET не должен менять данные на сервере.  |
| **STAT-5** | Получение статистики по несуществующему ID (v1 и v2) | Создано объявление, известен его ID. | 1. Удалить объявление: DELETE `/api/2/item/{id}`.<br>2. Отправить GET-запрос на `/api/1/statistic/{id}` (v1).<br>3. Отправить GET-запрос на `/api/2/statistic/{id}` (v2). | Статус-код 404 для обеих версий.  |
| **STAT-6** | Получение статистики с невалидным форматом ID (v1 и v2) | - | 1. Отправить GET-запрос на `/api/1/statistic/invalid-id` (v1).<br>2. Отправить GET-запрос на `/api/2/statistic/invalid-id` (v2). | Статус-код 400 для обеих версий.  |
| **STAT-7** | GET /statistic/:id возвращает объект, а не массив | Создано объявление, известен его ID. | 1. Отправить GET-запрос на `/api/1/statistic/{id}`.<br>2. Проверить тип корневого элемента ответа. | Статус-код 200. Тело ответа является JSON-объектом `{...}`, а не массивом `[{...}]`.  |
| **STAT-8** | Получение статистики по случайному несуществующему ID (v1 и v2) | - | 1. Сгенерировать случайный UUID.<br>2. Отправить GET-запрос на `/api/1/statistic/{uuid}` (v1).<br>3. Отправить GET-запрос на `/api/2/statistic/{uuid}` (v2). | Статус-код 404 для обеих версий.  |
| **STAT-9** | GET /item/:id не изменяет счетчик просмотров | Создано объявление, известен его ID и начальный viewCount. | 1. Запросить статистику через GET `/api/1/statistic/{id}`. Запомнить viewCount.<br>2. Запросить объявление: GET `/api/1/item/{id}`.<br>3. Снова запросить статистику: GET `/api/1/statistic/{id}`.<br>4. Сравнить viewCount до и после. | viewCount не изменился после GET /item/:id; запрос не должен менять данные на сервере.  |


---

## Удаление объявления - DELETE /api/2/item/:id


| ID | Заголовок | Предусловия | Шаги | Ожидаемый результат  |
| --------- | ------------------------------------------------------- | ----------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------  |
| **DEL-1** | Удаление существующего объявления | Создано объявление, известен его ID. | 1. Отправить DELETE-запрос на `/api/2/item/{id}`. | Статус-код 200. Объявление удалено.  |
| **DEL-2** | Объявление недоступно после удаления | Создано объявление, известен его ID. | 1. Удалить объявление: DELETE `/api/2/item/{id}`.<br>2. Попытаться получить объявление: GET `/api/1/item/{id}`. | DELETE возвращает 200. Последующий GET возвращает 404.  |
| **DEL-3** | Повторное удаление одного объявления | Создано объявление, известен его ID. | 1. Удалить объявление: DELETE `/api/2/item/{id}`.<br>2. Отправить повторный DELETE-запрос на `/api/2/item/{id}`. | Первый DELETE возвращает 200. Повторный DELETE возвращает 404.  |
| **DEL-4** | Удаление с невалидным форматом ID | - | 1. Отправить DELETE-запрос на `/api/2/item/invalid-id`. | Статус-код 400.  |
| **DEL-5** | Удаление объявления по случайному несуществующему UUID | - | 1. Сгенерировать случайный UUID.<br>2. Отправить DELETE-запрос на `/api/2/item/{uuid}`. | Статус-код 404. Объявление с таким ID не существует.  |
| **DEL-6** | После удаления объявление отсутствует в списке продавца | Создано объявление, известен его ID и sellerID. | 1. Удалить объявление: DELETE `/api/2/item/{id}`.<br>2. Получить список объявлений продавца: GET `/api/1/{sellerID}/item`.<br>3. Проверить, что ID удалённого объявления отсутствует в списке. | DELETE возвращает 200. GET /:sellerID/item не содержит ID удалённого объявления.  |


---

## HTTP-контракт


| ID | Заголовок | Предусловия | Шаги | Ожидаемый результат  |
| ---------- | ------------------------------------------------------ | ------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------- | -----------------------------------------------------------------------  |
| **HTTP-1** | Время ответа < 2 сек и Content-Type = application/json | Создано объявление, известен его ID. | 1. Выполнить запрос к каждому эндпоинту API (POST, GET, DELETE).<br>2. Замерить время ответа.<br>3. Проверить заголовок Content-Type. | Время ответа < 2 секунд. Content-Type содержит `application/json`.  |
| **HTTP-2** | PUT/PATCH запросы возвращают 405 | - | 1. Отправить PUT-запрос на каждый эндпоинт API.<br>2. Отправить PATCH-запрос на каждый эндпоинт API.<br>3. Проверить статус-код ответа. | Статус-код 405 (Method Not Allowed) для каждого запроса.  |
| **HTTP-3** | POST с неподдерживаемым Content-Type | - | 1. Отправить POST-запрос на `/api/1/item` с Content-Type `text/xml`.<br>2. Отправить POST-запрос с Content-Type `text/plain`. | Статус-код 400 (Bad Request).  |
| **HTTP-4** | Схема ошибочных ответов соответствует контракту | - | 1. Отправить запросы, дающие 400 и 404 на каждом эндпоинте.<br>2. Проверить тело ответа по JSON-схемам ошибок из проекта. | Структура JSON соответствует схеме для данного статус-кода и эндпоинта.  |
| **HTTP-5** | POST /api/1/item с пустым телом (без контента) | - | 1. Отправить POST-запрос на `/api/1/item` с заголовком `Content-Type: application/json` и пустым телом (без контента). | Статус-код 400 (Bad Request).  |
| **HTTP-6** | POST /api/1/item с невалидным JSON | - | 1. Отправить POST-запрос на `/api/1/item` с заголовком `Content-Type: application/json` и телом `{"invalid json`. | Статус-код 400 (Bad Request).  |


---

## E2E-сценарии


| ID | Заголовок | Предусловия | Шаги | Ожидаемый результат  |
| --------- | --------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------  |
| **E2E-1** | Полный жизненный цикл объявления | - | 1. Создать объявление: POST `/api/1/item` с валидным телом. Запомнить ID из ответа.<br>2. Получить объявление: GET `/api/1/item/{id}`. Сверить данные с шагом 1.<br>3. Получить статистику: GET `/api/1/statistic/{id}`. Сверить с данными создания.<br>4. Удалить объявление: DELETE `/api/2/item/{id}`.<br>5. Проверить удаление: GET `/api/1/item/{id}`. | Шаг 1: статус 200, объявление создано. Шаг 2: статус 200, данные совпадают. Шаг 3: статус 200, статистика совпадает. Шаг 4: статус 200. Шаг 5: статус 404, объявление не найдено.  |
| **E2E-2** | Кросс-версия: создание через v1, статистика и удаление через v2 | - | 1. Создать объявление: POST `/api/1/item`. Запомнить ID.<br>2. Получить объявление: GET `/api/1/item/{id}`. Сверить данные с шагом 1.<br>3. Получить статистику через v2: GET `/api/2/statistic/{id}`. Сверить с данными создания.<br>4. Удалить через v2: DELETE `/api/2/item/{id}`. | Шаг 1: статус 200. Шаг 2: статус 200, данные объявления совпадают с отправленными. Шаг 3: статус 200, статистика совпадает. Шаг 4: статус 200, объявление удалено.  |
| **E2E-3** | Жизненный цикл нескольких объявлений продавца | - | 1. Выбрать sellerID (например, 333333).<br>2. Создать 3 объявления с этим sellerID через POST `/api/1/item`. Запомнить ID каждого.<br>3. Получить список объявлений продавца: GET `/api/1/333333/item`.<br>4. Убедиться, что все 3 объявления присутствуют в списке.<br>5. Удалить все 3 объявления по ID через DELETE `/api/2/item/{id}`.<br>6. Повторно запросить список объявлений продавца: GET `/api/1/333333/item`. Убедиться, что ни один из удалённых ID не присутствует в ответе. | Шаг 2: статус 200 для каждого создания. Шаг 3: статус 200, список содержит все 3 объявления. Шаг 5: статус 200 для каждого удаления. Шаг 6: статус 200, удалённые объявления отсутствуют в списке продавца.  |


